{% extends 'base.html.twig' %}

{% block title %}FoodCategory{% endblock %}

{% block body %}
    <h1>FoodCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ food_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ food_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ food_category.description }}</td>
            </tr>
            <tr>
                <th>Short</th>
                <td>{{ food_category.short }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_menu_food_category_index') }}">back to list</a>

    <a href="{{ path('app_menu_food_category_edit', {'id': food_category.id}) }}">edit</a>
{% endblock %}
